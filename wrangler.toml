name = "iroofer-contractors-web"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "out"

# Do not put NEXT_PUBLIC_TURNSTILE_SITE_KEY in [vars] — it conflicts with the
# Pages dashboard secret of the same name. Site key is baked via .env.production
# at Next build time. TURNSTILE_SECRET_KEY stays a Pages secret only.
